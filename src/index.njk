---
layout: layouts/home.njk
title: Úvod
---
<section>
  <div class="container flex-grid cards">
    {%- for entry in collections.all | eleventyNavigation %}
        <a class="col col-half card" href="{{ entry.url | url }}">
            {% icon entry.icon, 'theme-icon' %}
            <h3>{{ entry.title }}</h3>
            <p>{{ entry.excerpt }}</p>
        </a>
    {%- endfor %}
  </div>
</section>
